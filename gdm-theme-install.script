#!/bin/bash

#Used directory
gnomedir=/usr/share/gnome-shell
themedir=/usr/share/themes/Adapta-Breath/gnome-shell
theme=gnome-shell-theme.gresource

#Install GDM theme

cd $gnomedir

if [ -f $theme ]; then
   mv $theme $theme.old
fi

cp $themedir/$theme $gnomedir/$theme

exit 0


