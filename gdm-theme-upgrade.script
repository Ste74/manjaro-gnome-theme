#!/bin/bash

#Used directory
gnomedir=/usr/share/gnome-shell
themedir=/usr/share/themes/Adapta-Breath/gnome-shell
theme=gnome-shell-theme.gresource

#Upgrade GDM theme

cmp $gnomedir/$theme $themedir/$theme 

if [ $? == 1 ]; then
   cp -f $themedir/$theme $gnomedir/$theme
fi

exit 0
