#!/usr/bin

#Used directory
gnome-dir=/usr/share/gnome-shell/
theme-dir=/usr/share/themes/Adapta-Breath/gnome-shell
theme=gnome-shell-theme.gresource

#Upgrade GDM theme

cmp $gnome-dir/$theme $theme-dir/$theme 

if [ $? == 1 ]; then
   cp -f $theme-dir/$theme $gnome-dir/$theme
fi

exit 0
